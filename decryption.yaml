apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: my-secrets
  namespace: flux-system
spec:
  interval: 5m
  path: ./TodoApp/manifests
  sourceRef:
    kind: GitRepository
    name: todoapp-repo
  decryption:
    provider: sops
    secretRef:
      name: sops-age